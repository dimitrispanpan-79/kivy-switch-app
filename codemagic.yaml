workflows:
  android-build:
    name: Android Build
    max_build_duration: 60
    environment:
      vars:
        ANDROID_HOME: /opt/android-sdk-linux
    scripts:
      - echo "Starting build"
      - buildozer android debug
    artifacts:
      - app/bin/*.apk
    publishing:
      email:
        recipients:
          - dimitrispanpan@gmail.com  # optional: replace with your email
